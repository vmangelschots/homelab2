---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: piwigo-mysql
  namespace: broek
  labels:
    app.kubernetes.io/name: piwigo-mysql
    app.kubernetes.io/instance: piwigo-mysql
    app.kubernetes.io/part-of: piwigo
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: piwigo-mysql
      app.kubernetes.io/instance: piwigo-mysql
  template:
    metadata:
      labels:
        app.kubernetes.io/name: piwigo-mysql
        app.kubernetes.io/instance: piwigo-mysql
    spec:
      containers:
        - name: mysql
          image: mariadb:10.11
          imagePullPolicy: IfNotPresent
          securityContext:
            runAsUser: 999
            runAsGroup: 999
          ports:
            - name: mysql
              containerPort: 3306
              protocol: TCP
          env:
            - name: MYSQL_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: piwigo-secret
                  key: mysql-root-password
            - name: MYSQL_DATABASE
              value: "piwigo"
            - name: MYSQL_USER
              value: "piwigo"
            - name: MYSQL_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: piwigo-secret
                  key: mysql-password
          volumeMounts:
            - name: data
              mountPath: /var/lib/mysql
          readinessProbe:
            exec:
              command:
                - sh
                - -c
                - mysqladmin ping -u root -p${MYSQL_ROOT_PASSWORD}
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
          livenessProbe:
            exec:
              command:
                - sh
                - -c
                - mysqladmin ping -u root -p${MYSQL_ROOT_PASSWORD}
            initialDelaySeconds: 60
            periodSeconds: 20
            timeoutSeconds: 5
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 1000m
              memory: 1Gi
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: piwigo-mysql-data
      terminationGracePeriodSeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: piwigo-mysql-svc
  namespace: broek
  labels:
    app.kubernetes.io/name: piwigo-mysql
spec:
  selector:
    app.kubernetes.io/name: piwigo-mysql
    app.kubernetes.io/instance: piwigo-mysql
  ports:
    - name: mysql
      protocol: TCP
      port: 3306
      targetPort: 3306
  type: ClusterIP
